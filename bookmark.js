javascript: (function () { const timer = setInterval(function () { let plus = document.querySelector('button.btn-default.plus:not([disabled])'); if (plus) { plus.click(); plus.click(); } let agree = document.querySelector('input#person_agree_terms'); if (agree && !agree.checked) agree.click(); let memberInput = document.querySelector('input[ng-model="oq.member_code"]'); if (memberInput && !memberInput.disabled) { memberInput.value = MEMBER_CODE; memberInput.dispatchEvent(new Event('input', { bubbles: true })); } let next = [].slice.call(document.querySelectorAll('button')).find(b => b.textContent.trim() === '下一步'); if (next) { next.click(); clearInterval(timer); } }, 50); }());


javascript: (function () { const SEAT = '紫1C'; const timer = setInterval(function () { const unit = Array.from(document.querySelectorAll('.ticket-unit')).find(u => { const nameEl = u.querySelector('.ticket-name'); console.log(nameEl.textContent.trim()); return nameEl; }); if (!unit) return; const plus = unit.querySelector('button.btn-default.plus:not([disabled])'); if (plus) { plus.click(); plus.click(); console.log('[搶票] 已選', SEAT); } const agree = document.querySelector('input#person_agree_terms'); if (agree && !agree.checked) agree.click(); const next = [].slice.call(document.querySelectorAll('button')).find(b => b.textContent.trim() === '下一步'); if (next) { next.click(); clearInterval(timer); } }, 50); })();